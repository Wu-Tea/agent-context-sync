interface:
  display_name: "Agent Context Sync"
  short_description: "Sync AI handoffs and decisions locally"
  default_prompt: "Use $agent-context-sync to update local project context, record decisions, prepare subagent briefs, and keep platform memory clean."

policy:
  allow_implicit_invocation: true
